package controller;

import model.Continents;
import model.Player;
/**
 * This class intend to calculate the no. of bonus armies in reinforcement phase
 * @param takes player and continent objects to calculate the armies based on the parameters
 * @return returns total armies obtained as integer
 * @author sakib
 *
 */

public class ReinforcementController {
/*
	public int reinforceArmy(Player player, Continents continent) {
		int countryReward = 0;
		int continentReward = 0;
		int cardReward = 0;
		int total = 0;
		countryReward = (int)Math.floor(Player.occupiedCountries / 3);
		if (player.getCountires == continent.getCountires) { // if has same elements in both object
			continentReward = Integer.ParseInt(Continents.continentControlValue);
		}
		total = countryReward + continentReward;
		if (total >= 3) {
			return total;
		}else {
			return 3; // ???? confused
		}
	}
	*/
	
}
